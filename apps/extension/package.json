{
  "name": "@redactly/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Redactly browser extension — mask PII locally inside ChatGPT & Claude.",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc -p . --noEmit"
  },
  "dependencies": {
    "@redactly/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.3.0"
  }
}
